<?php  if ( ! defined('BASEPATH')) exit('No direct script access allowed');

// Translates internal language names to the name of the associated flag image
$config["flagCodes"] = array(
    "english" => "gb",
    "dutch" => "nl"
    );

// Translates browser reported language codes to internal language names
$config["browserCodes"] = array(
    "nl" => "dutch",
    "en" => "english"
    );