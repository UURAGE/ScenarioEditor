/* © Utrecht University and DialogueTrainer */

// In this file you can find most of the constants
/* exported Constants */
let Constants;

(function()
{
    "use strict";

    // Values for the meta dialog
    const heightMetaScreen = 600;
    const widthMetaScreen = 750;

    // Values for the import dialog
    const heightImportScreen = 500;
    const widthImportScreen = 500;

    // Values for the parameter dialog
    const heightParameterScreen = 768;
    const widthParameterScreen = 1024;

    // eslint-disable-next-line no-global-assign
    Constants =
    {
        heightMetaScreen: heightMetaScreen,
        widthMetaScreen: widthMetaScreen,
        heightImportScreen: heightImportScreen,
        widthImportScreen: widthImportScreen,
        heightParameterScreen: heightParameterScreen,
        widthParameterScreen: widthParameterScreen
    };
})();
